@model Annonce_Asp.netCore.Models.Categories

@{
    ViewData["Title"] = "Delete";
}

<h3  class="h3 mb-2 text-gray-800 alert alert-danger">Are you sure you want to delete this?</h3>
<div>
    <h4>Categories</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nom_Cat)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.nom_Cat)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index" class="btn btn-outline-danger">Back to List</a>
    </form>
</div>